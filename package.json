{
  "name": "fretboard-chord-search",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "smoke": "node scripts/smoke-dom.mjs",
    "check": "tsc --noEmit && vitest run && vite build && node scripts/smoke-dom.mjs"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "typescript": "~5.9.2",
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  }
}
